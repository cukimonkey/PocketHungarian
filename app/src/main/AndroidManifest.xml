<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.pockethungarian">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo01"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BasicActivity" />
        <activity android:name=".Family" />
        <activity android:name=".House" />
        <activity android:name=".ColorsNumbers" />
        <activity android:name=".Grocery" />
        <activity android:name=".EatingOut" />
        <activity android:name=".Accommodation"></activity>
    </application>

</manifest>